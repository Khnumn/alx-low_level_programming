#include <stdio.h>
#include <unistd.h>
/**
 * main - Initialization
 * Desc: print single numbers
 * Return: 0 success
 */
int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
