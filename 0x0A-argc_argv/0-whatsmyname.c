#include "main.h"
#include <stdio.h>

/**
 * main - Name of the program
 *@argc: number of command line arguments
 *@argv: Array name
 * Return: 0 success
 */
int main (int argc, char *argv[])
{
	printf("%s\n", argc[argv * 0]);

	return (0);
}
